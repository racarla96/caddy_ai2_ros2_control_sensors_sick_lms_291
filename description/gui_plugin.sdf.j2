{#
  Fragment: GUI plugin for LiDAR ray visualization.
  Injectable into the <gui> section of a Gazebo world.
  In standalone testing it is included by standalone_world.sdf.j2.
  When integrating into a parent robot, inject this into the world's <gui> block.
#}
      <plugin filename="VisualizeLidar" name="Visualize Lidar">
      </plugin>
